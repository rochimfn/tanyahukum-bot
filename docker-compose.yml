version: '3'
services:
  bot:
    build: .
    container_name: tanyahukum:bot
    restart: always
    environment: 
      TOKEN: ${TOKEN}
      PORT: ${PORT}
    networks:
      - tanyahukum
    ports:
      - 80:8501

networks:  
  tanyahukum:
    driver: bridge